general:
  cfApiEndpoint: "https://api.cf.us10.hana.ondemand.com"
  cfOrg: "Adoption_and Consumption COE_ABAPWorkshop"
  cfSpace: "dev"
  cfCredentialsId: "cfAuthentification"
  cfServiceInstance: "abap"
  repositories: "repositories.yml"
stages:
  Clone Repositories:
    strategy: "Pull"
  ATC:
    atcConfig: "atcConfig.yml"
  AUnit:
    # In order to be executed, the AUnit stage needs at least one configuration entry
    # If the AUnit stage should not be executed, delete the whole section
    execute: stage
